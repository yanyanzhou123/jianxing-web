{
  "name": "jianxing",
  "type": "module",
  "version": "1.2.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy --project-name=jianxing --branch main"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  }
}
